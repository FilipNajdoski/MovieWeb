@model MovieWeb.Data.Entities.Genre

@{
    ViewData["Title"] = "Details";
}

    <div class="container">
        <h4>Genre Details</h4>
        <hr />
        <div class="row">
            <div class="col-md-4">
                <div class="d-flex justify-content-between">
                    <div class="font-weight-bold">
                        @Html.DisplayNameFor(model => model.GenreName)
                    </div>
                    <div>
                        @Html.DisplayFor(model => model.GenreName)
                    </div>
                </div>   
                <hr />
                <div class="d-flex justify-content-between">
                    <a asp-action="Edit" asp-route-id="@Model.GenreID" class="btn btn-primary "><i class="fas fa-edit"></i>Edit</a>
                    <a asp-action="Index" class="btn btn-success"><i class="fas fa-arrow-alt-circle-left"></i>Back to List</a>
                </div>
            </div>
        </div>

        
    </div>
